server:
  servlet:
    context-path: /
  port: 8080

spring:
  application:
    name: PetShop
  datasource:
    url: jdbc:h2:mem:dbTeste
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true